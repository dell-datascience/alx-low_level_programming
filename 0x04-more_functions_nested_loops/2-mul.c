#include <ctype.h>
#include "main.h"

/*
 * mul - multipies two integers
 *
 * @a: parameter one
 * @b: parameter two
 *
 * Return: return the product of two parameters
 */
int mul(int a, int b)
{
	int result;

	result = (a * b);

	return (result);
}
