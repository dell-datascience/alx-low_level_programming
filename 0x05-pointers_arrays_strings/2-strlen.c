#include "main.h"
#include <string.h>

/**
 * _strlen - calculates length of string
 * @s: parameter one
 *
 * Return: integer length of string
 */
int _strlen(char *s)
{
	int result;

	result = strlen(s);

	return (result);
}
